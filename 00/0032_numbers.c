#include<stdio.h>
main()
{
    int n;
    scanf("%d",&n);
    int a[n];
    int i,j,k;
    for(i=0;i<n;i++)
    {
        a[i]=0;
        scanf("%d",&a[i]);
    }
    for(i=0;i<n;i++)
    {
        for(j=i+1;j<n;j++)
        {
            if(a[i]>a[j])
            {
                k=a[i];
                a[i]=a[j];
                a[j]=k;
            }
        }
    }
    k=0;
    for(i=0;i<n;i++)
    {
        if(a[i]==0)
        {
            k++;
        }
    }
    if(k>0)
    {
        j=a[k];
        a[0]=j;
        a[k]=0;
    }
    for(i=0;i<n;i++)
    {
        printf("%d",a[i]);
    }
}
